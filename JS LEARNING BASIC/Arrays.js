/* 
 1. Array is also like the object we can store the multiple 
 value without any key value
*/ 

// Example 
let colors = ["blue","black","white"] // It represent the arrays

// console.log(colors);

// If i need only the  black color 
console.log(colors[1]);




//  if we add a new colors into the colors we can
colors[3] = "violet" // Also we can delete it and update it
// console.log(colors.indexOf("white"));
